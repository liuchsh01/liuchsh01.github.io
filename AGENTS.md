# AGENTS.md

## 项目定位

这是一个面向日常使用的纯静态小工具集合，包含但不限于：

- Hash、编码、加密与格式转换工具；
- 计算器、税费、比例等计算工具；
- 时间戳、日期与时区工具；
- 文本筛选、整理与批处理工具。

项目应保持轻量、直接、可离线理解。除确有必要的第三方能力外，工具优先使用原生 HTML、CSS 和 JavaScript 实现，不引入构建步骤、前端框架或后端服务。

所有页面（包括主页）必须采用统一视觉语言。`assets/css/theme.css` 是主题实现的唯一基准，`tools/20260807_multiline-calculator/` 是标准工具页的布局参考；新增或重构页面应复用它们的颜色、排版、留白、圆角、阴影、表单和交互风格，而不是自行设计另一套主题。

## 核心原则

1. **一个页面解决一个明确问题。** 工具应开箱即用，避免账户、引导流程和不必要配置。
2. **本地优先。** 用户输入和计算默认只在浏览器中处理；未经明确说明不得上传数据。
3. **风格统一。** 主页与所有工具页共享设计令牌和基础组件。
4. **移动端可用。** 从 320px 宽度开始保证无横向滚动、控件可点击、内容可读。
5. **结果可信。** 对输入做校验，明确展示错误、空状态和成功反馈，不静默失败。
6. **易于维护。** 文件命名、目录、页面骨架和交互模式保持一致；避免复制后逐页漂移。

## 目录规范

目标目录结构如下：

```text
/
├── AGENTS.md
├── index.html
├── assets/
│   ├── css/
│   │   └── theme.css
│   ├── js/
│   │   └── common.js
│   └── icons/
├── tools/
│   ├── yyyyMMdd_multiline-calculator/
│   │   ├── index.html
│   │   ├── style.css        # 仅在工具存在较多专属样式时创建
│   │   └── script.js        # 仅在工具存在较多专属逻辑时创建
│   └── yyyyMMdd_timestamp-converter/
│       └── index.html
└── tests/
    └── site-structure.sh
```

### 工具目录命名

- 工具目录格式必须为 `yyyyMMdd_<english-short-name>`。
- `yyyyMMdd` 是工具首次加入项目的日期，例如 `20260810_timestamp-converter`。
- 日期与英文短名之间使用一个下划线 `_`。
- 英文短名只使用小写 ASCII 字母、数字和连字符 `-`，不得使用空格、中文或下划线。
- 名称应简短、明确、表达工具用途，例如：
  - `20260810_hash-generator`
  - `20260810_multiline-calculator`
  - `20260810_timestamp-converter`
- 首次创建后不得因普通修改而更新日期前缀，避免 URL 不稳定。
- 工具改名或迁移后，根目录不保留旧工具 HTML；只有当前任务明确要求兼容旧 URL 时才创建跳转页。

### 文件放置规则

- 根目录 `index.html` 是唯一主页。
- 实际工具实现统一放在 `tools/<工具目录>/index.html`，根目录不再新增完整工具页。
- 共享视觉样式放在 `assets/css/theme.css`，共享的复制、Toast 等通用逻辑可放在 `assets/js/common.js`。
- 工具独有代码优先与工具放在同一目录；内容较少时可内嵌在该工具的 `index.html`，内容明显增长时拆分为 `style.css` 和 `script.js`。
- 图片和图标使用可读英文文件名；真正跨页面复用的资源放入 `assets/icons/`，工具专属资源放在对应工具目录内。
- 页面路径必须使用相对链接，确保 GitHub Pages 子路径部署可用。禁止硬编码本机路径或站点根绝对路径。

### 现有页面迁移

- `index.html` 始终保留在根目录，并按本文主页规范重构。
- 当前位于根目录的工具页应迁入 `tools/yyyyMMdd_<english-short-name>/`，主页链接更新并完成验证后删除原工具 HTML。
- 根目录除 `index.html` 外不得保留工具实现或旧工具跳转文件。
- 不为执行本次迁移而顺带改写工具算法；目录/视觉调整与业务逻辑修复应尽量分开。

## 统一主题规范

主题以 `assets/css/theme.css` 为唯一视觉实现基准，页面通过 CSS 变量和通用类复用；`assets/js/common.js` 负责主题选择、持久化和跨页面恢复；`tools/20260807_multiline-calculator/` 可作为标准双栏工具页参考。

### 设计令牌

默认主题为 `jade`（翡翠青），基础颜色保持以下语义和默认值：

```css
:root {
  color-scheme: light;
  --ink: #15231f;
  --muted: #60726c;
  --line: #d9e6e0;
  --paper: #ffffff;
  --mist: #f4f8f6;
  --teal: #0f766e;
  --teal-dark: #0b5f59;
  --teal-pale: #e3f3ee;
  --error: #bd3f3f;
  --error-pale: #fff0ef;
  --shadow: 0 18px 50px rgba(17, 52, 44, 0.11);
}
```

全站固定支持以下 5 个主题 ID：

| 主题 ID | 中文名称 | 模式 | 用途 |
| --- | --- | --- | --- |
| `jade` | 翡翠青 | 浅色 | 默认主题，沿用项目原有青绿色视觉 |
| `ocean` | 海雾蓝 | 浅色 | 低饱和蓝灰与海蓝强调色 |
| `sand` | 暖砂金 | 浅色 | 暖白、砂岩与低饱和金棕色 |
| `rose` | 雾玫瑰 | 浅色 | 暖灰、雾粉与低饱和玫瑰色 |
| `night` | 深林夜色 | 深色 | 深绿黑背景与浅薄荷强调色 |

- 默认令牌定义在 `:root`，其余主题使用 `:root[data-theme="<id>"]` 覆盖同一组语义变量。
- 主题应形成明确的视觉性格，可以统一改变语义颜色、标题字体、圆角体系、阴影质感、页面背景纹理和 `color-scheme`；不得改变工具布局、内容顺序、控件位置或功能。
- 新增主题时必须完整覆盖文字、背景、边框、控件、强调色、状态色、语法色、Focus 和阴影令牌，不能只替换主色。
- 工具页不得声明自己的主题或硬编码独立主色；领域 CSS 始终引用 `theme.css` 中的语义变量。
- `common.js` 使用 `toolbox-theme` 保存主题，并把主题 ID 设置到 `<html data-theme>`；所有页面必须在共享 CSS 后同步加载 `common.js`，避免切页时先闪现默认主题。
- 主题选择器只需在主页提供，但用户选择必须在主页与全部工具页之间保持一致。

### 页面背景与容器

- `body` 使用当前主题的 `--mist` 底色、`--body-pattern` 纹理和 `--body-glow-start`、`--body-glow-end` 径向渐变。
- 页面最小宽度为 `320px`，最小高度为 `100vh`，默认无外边距。
- 主容器使用 `.shell`：桌面端最大宽度约 `1100px`，左右至少保留 `16px` 空间。
- 核心内容使用 `--paper` 卡片、`--panel-line` 边框、主题圆角令牌和 `--shadow`；不得假设卡片一定是白色或固定为 `22px` 圆角。
- 卡片内部以 `20px`–`28px` 间距为主，不使用拥挤或装饰性过强的布局。

### 字体与标题

- 正文字体使用系统无衬线字体栈：`-apple-system`、`BlinkMacSystemFont`、`Segoe UI`、`PingFang SC`、`Microsoft YaHei`、`sans-serif`。
- 页面主标题使用 `Georgia, "Songti SC", serif`，保持参考页的编辑感。
- 页面标题使用响应式字号，建议 `clamp(34px, 5vw, 52px)`。
- 每页标题区包含：
  - 小号大写英文 eyebrow；
  - 简洁明确的中文 `h1`；
  - 一段说明用途和支持范围的副标题。
- 辅助文字使用 `--muted`，正文行高建议不低于 `1.6`。
- 数值、Hash、密钥、时间戳和代码类内容使用等宽字体。

### 通用组件

- 主操作按钮使用 `--teal` 背景、白色文字和约 `10px` 圆角；Hover 使用 `--teal-dark`。
- 次要按钮使用白色背景、`--line` 边框；Hover 时边框和文字切换为青绿色。
- 输入框、选择框和文本域使用浅色背景、`--line` 边框和约 `10px`–`12px` 圆角。
- Focus 状态必须可见，推荐使用青绿色边框和 `rgba(15, 118, 110, 0.11)` 外环。
- 结果区使用独立面板或逐行卡片；结果较长时允许换行或滚动，不能撑破页面。
- 错误状态使用 `--error` 和 `--error-pale`，错误信息应说明用户可以如何修正。
- 复制、保存等短反馈统一使用底部居中的 Toast，并提供 `role="status"` 和 `aria-live="polite"`。
- 动效只用于 Hover、Focus、Toast 和轻量内容出现，通常控制在 `160ms`–`300ms`；尊重 `prefers-reduced-motion`。

### 响应式规则

- 桌面端可使用双栏工作区：输入在左、结果在右。
- `760px` 以下切换为单栏，卡片圆角和内边距适当减小。
- 操作按钮在窄屏可以换行或撑满容器，但点击区域高度不低于 `44px`。
- 不依赖 Hover 才能发现关键操作；长文本、表格和结果必须能在移动端正常查看。

## 主页规范

主页负责解释站点用途并提供全部工具入口，不承载具体工具逻辑。

### 页面结构

主页建议使用以下顺序：

1. `.shell` 主容器；
2. `.masthead` 标题区；
3. 简短站点说明；
4. 工具卡片网格；
5. 简洁页脚。

标题区应与工具页使用相同的 eyebrow、`h1` 和 subtitle 结构。主页标题可使用“实用工具集”，说明文字强调“轻量、浏览器本地处理、无需登录”。

### 工具卡片

- 每个工具必须有一张完整可点击或包含明确入口的卡片。
- 卡片至少展示：工具名称、一句话用途、分类标签和访问入口。
- 分类名称保持稳定，优先使用：`计算`、`时间日期`、`文本`、`编码与安全`、`开发辅助`。
- 卡片采用 `--paper`、`--line`、统一圆角和轻量阴影；Hover 只做轻微上移、边框或阴影变化。
- 工具排序默认按分类组织，同类内按常用程度排序；不得仅按文件名或创建日期展示。
- 新增、迁移或移除工具时必须同步更新主页入口，且链接指向 `tools/yyyyMMdd_<english-short-name>/`。
- 暂不实现没有明确需求的搜索、收藏、登录、统计或复杂导航。

## 工具页规范

### 页面骨架

每个工具页至少包含：

1. 标准 `<head>`：`charset`、`viewport`、`theme-color`、准确的 `title` 和必要的 description；
2. 与主页一致的 `.shell` 和 `.masthead`；
3. 返回主页的清晰链接；
4. 一个主要 `.workspace`，承载输入、操作和结果；
5. 必要的说明、限制、来源或隐私提示；
6. Toast 或页面内状态区域。

复杂工具可以使用多面板布局，但一个页面只保留一个视觉主任务。说明性长文放在工作区之后，避免把核心操作推到首屏之外。

### 文案与状态

- 页面语言统一为简体中文，专业缩写可保留英文。
- 标题说明“这是什么”，副标题说明“能做什么”；按钮直接描述动作，如“生成 Hash”“转换时间”“计算全部”。
- 输入控件必须有可见 `<label>`；placeholder 只给示例，不代替标签。
- 页面必须覆盖初始空状态、输入错误、处理成功和不可用状态。
- 不使用 `alert()` 作为常规校验方式，错误应显示在相关区域或统一状态提示中。
- 对时间、金额、编码和小数精度等容易误解的行为，在结果附近明确单位、时区、舍入方式或格式。

### 交互与可访问性

- 优先使用语义化的 `main`、`header`、`section`、`form`、`label`、`button` 和 `output`。
- 所有功能必须可通过键盘完成；焦点顺序与视觉顺序一致。
- 图标按钮必须有 `aria-label`，切换按钮必须维护 `aria-pressed`。
- 动态结果使用 `aria-live="polite"`，但避免每次输入都产生过度播报。
- 支持高频工具的合理快捷键，例如 `Ctrl/Cmd + Enter` 执行；页面上要展示快捷键提示。
- 复制操作优先使用 Clipboard API，并提供兼容降级和明确反馈。

## HTML、CSS 与 JavaScript 约定

### HTML

- 使用 `<!doctype html>` 和 `<html lang="zh-CN">`。
- 页面在共享 `theme.css` 后同步加载 `assets/js/common.js`，不得给该脚本添加 `defer`；工具独有脚本继续使用 `defer` 或 ES modules。
- 保持语义化结构，不使用大量无意义嵌套。
- 不使用内联 `onclick` 等事件属性；事件统一在脚本中注册。
- 外部链接使用 HTTPS；新窗口打开时添加 `rel="noopener noreferrer"`。

### CSS

- 共享视觉规则优先写入 `assets/css/theme.css`，工具 CSS 只覆盖布局和领域组件。
- 使用已有 CSS 变量，不在工具页重复硬编码主题色、阴影和基础圆角。
- 任一新语义令牌必须先在默认主题和其余 4 个主题中同时定义，再由组件引用。
- 深色主题下不得依赖浏览器默认白色背景或黑色文字；表单、代码块、表格、Canvas 外框和空状态必须显式使用语义变量。
- 类名使用小写 kebab-case，并按组件含义命名，不按颜色或临时位置命名。
- 禁止通过 `!important` 解决正常层叠问题，`[hidden]` 等明确工具类除外。

### JavaScript

- 默认使用原生 JavaScript；简单页面可使用 IIFE，拆分脚本时使用 `defer` 或 ES modules。
- 主题状态统一通过 `window.Toolbox.getTheme()`、`window.Toolbox.setTheme()` 或 `toolbox-theme-change` 事件读取，不得由工具脚本另建主题存储。
- 查询 DOM 后缓存常用元素，事件使用 `addEventListener` 注册。
- 不使用 `eval()`、`new Function()` 或将用户输入拼接成可执行代码。
- 用户输入必须先归一化和校验，再进入计算或转换逻辑。
- 业务计算尽量拆成无 DOM 依赖的纯函数，便于测试和复核。
- 错误信息面向用户，控制台信息面向开发者；不得把敏感输入写入日志。
- 浏览器原生能力足够时不引入依赖；确需 CDN 依赖时锁定明确版本、使用 HTTPS，并在页面中说明其用途。

## 新增工具流程

新增工具时按以下顺序执行：

1. 明确工具的单一用途、输入、输出、边界和错误情况。
2. 使用首次创建日期和英文短名创建 `tools/yyyyMMdd_<english-short-name>/`。
3. 从统一工具页骨架开始，实现主题、响应式和可访问状态。
4. 实现并校验核心逻辑，避免把 UI 和计算逻辑完全耦合。
5. 在根目录 `index.html` 增加工具卡片和正确链接。
6. 删除迁移前的根目录旧工具 HTML；仅在当前任务明确要求时保留兼容跳转。
7. 完成下方验收清单后再提交。

## 完成验收清单

每次新增或修改页面至少检查：

- 页面可直接通过相对路径打开，GitHub Pages 子路径下链接正确；
- 主页和工具页视觉与 `assets/css/theme.css` 保持同一主题；
- `jade`、`ocean`、`sand`、`rose`、`night` 五个主题均检查文字对比度、表单、结果区、状态色、Focus 和移动端可读性；
- 从主页切换主题后进入任一工具页仍保持相同主题，刷新页面后选择不丢失；
- 桌面端和 320px–760px 窄屏均无意外横向滚动；
- 键盘可以完成主要操作，Focus 状态清晰；
- 空输入、非法输入、极端值和正常值都有合理结果；
- 动态结果、复制反馈和错误提示清晰且可访问；
- 页面无未捕获 JavaScript 错误，控制台无无意义日志；
- 新工具已加入主页，旧地址兼容策略已处理；
- 未引入不必要的框架、依赖、网络请求或构建步骤；
- 对算法、税率、时区等可能变化或影响准确性的规则注明依据和适用范围。

## 修改边界

- 处理某个工具时，不顺带重写其他工具的业务逻辑。
- 统一主题时可以提取共享样式和通用交互，但必须保证现有功能不退化。
- 涉及税务、加密、时间规则等高风险或易变化内容时，先核对权威来源，再修改计算逻辑。
- 如果本文与具体任务说明冲突，以用户在当前任务中的明确要求为准。

## Git 提交规范

- 功能开发完成且相关自测全部通过后，AI 可以无需再次征求用户确认，自行创建 Git 提交。
- 提交前必须检查 `git status` 和实际差异，仅暂存、提交当前任务直接涉及的文件，不得夹带用户已有改动或其他任务的未完成内容。
- 提交信息应简短、明确地说明本次变更；优先使用非交互式 Git 命令。
- 自测失败、变更范围不明确、存在冲突或无法确认文件归属时，不得自行提交，应先解决问题或向用户说明。
- 自行提交权限仅包含本地 `git commit`，不包含推送远端、强制推送、变基、打标签或改写历史；这些操作仍需用户明确要求。
- 提交完成后应向用户报告提交摘要、Commit Hash 和已执行的验证项目。
